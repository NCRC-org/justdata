"""BigQuery queries (placeholder).

The lender analysis orchestrator runs queries inline using
``execute_query`` from ``justdata.shared.utils.bigquery_client``.
This module exists for parity with the standard package layout and is
reserved for future query extraction.
"""
